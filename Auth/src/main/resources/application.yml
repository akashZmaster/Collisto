server:
  port: 8899

spring:
  application:
    name: API-AUTH
  config:
    import: nacos:api-chatgpt.yaml
  cloud:
    compatibility-verifier:
      enabled: false
    loadbalancer:
      ribbon:
        enabled: false
    nacos:
      server-addr: 10.10.10.10
      discovery:
        group: collisto
        metadata:
          version: v1
        namespace: test

ribbon:
  nacos:
    enabled: false
