server:
  port: 8899

spring:
  application:
    name: API-AUTH
  cloud:
    compatibility-verifier:
      enabled: false
    nacos:
      discovery:
        server-addr:
        group:
        metadata:
          version:
        namespace:

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: always


